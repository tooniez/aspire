<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.example</groupId>
    <artifactId>forecast</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>jar</packaging>

    <properties>
        <maven.compiler.release>21</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <opentelemetry-javaagent.version>2.24.0</opentelemetry-javaagent.version>
    </properties>

    <!--
        No <dependencies> block on purpose. The service is built on the JDK's own HTTP server, so the
        only thing the build downloads is the OpenTelemetry agent below. That keeps this playground
        cheap to build in CI, where it exists to prove that a single-file Java AppHost can orchestrate
        a Java service, not to demonstrate a web framework.
    -->
    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <configuration>
                    <archive>
                        <manifest>
                            <mainClass>com.example.forecast.ForecastService</mainClass>
                        </manifest>
                    </archive>
                </configuration>
            </plugin>

            <!--
                The agent is a runtime dependency of the JVM launch, not of the compile classpath, so it
                is copied next to the JAR instead of being declared as a <dependency>. withOtelAgentDefaultPath()
                in the AppHost expects it at exactly target/agent/opentelemetry-javaagent.jar.
                https://opentelemetry.io/docs/zero-code/java/agent/
            -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <executions>
                    <execution>
                        <id>copy-opentelemetry-javaagent</id>
                        <phase>initialize</phase>
                        <goals>
                            <goal>copy</goal>
                        </goals>
                        <configuration>
                            <artifactItems>
                                <artifactItem>
                                    <groupId>io.opentelemetry.javaagent</groupId>
                                    <artifactId>opentelemetry-javaagent</artifactId>
                                    <version>${opentelemetry-javaagent.version}</version>
                                    <outputDirectory>${project.build.directory}/agent</outputDirectory>
                                    <destFileName>opentelemetry-javaagent.jar</destFileName>
                                </artifactItem>
                            </artifactItems>
                            <overWriteIfNewer>true</overWriteIfNewer>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
