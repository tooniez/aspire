---
apiVersion: "v1"
kind: "ConfigMap"
metadata:
  name: "executable-config"
  labels:
    app.kubernetes.io/name: "{{ .Chart.Name }}"
    app.kubernetes.io/component: "executable"
    app.kubernetes.io/instance: "{{ .Release.Name }}"
data:
  DATA_PATH: "{{ .Values.config.executable.DATA_PATH }}"
